---
name: zhr-plan
description: |
  零幻觉调研 - 计划阶段（独立会话架构）。
  执行 Phase 0-2：历史知识读取 + 需求类型区分 + 计划制定。
  支持迭代需求和全新需求两种模式。
  符合原始 Ralph 设计：每个阶段是独立的上下文窗口。
user-invocable: true
allowed-tools:
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - AskUserQuestion
  - mcp__graphiti__search_nodes
  - mcp__graphiti__search_memory_facts
  - mcp__graphiti__get_episodes
  - mcp__graphiti__get_status
---

# 零幻觉调研 - 计划阶段 (Phase 0-2)

> **架构**: 独立上下文窗口（符合原始 Ralph 设计）
> **状态传递**: 通过 `.research/state.json` 文件系统

你是调研协调员，负责**仅执行计划阶段**。

---

## 🏗️ 独立会话架构说明

```
┌─────────────────────────────────────────────────────────────────────┐
│  Ralph 风格独立上下文窗口架构                                         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  会话1: /zhr-plan (当前)                                             │
│    Phase 0-1-2 → 写入 state.json + task_plan.md                      │
│    输出: "PHASE_COMPLETE: plan"                                      │
│                            ↓                                         │
│            ┌──────────────────────────────┐                         │
│            │    文件系统 (状态传递)         │                         │
│            │  - .research/state.json      │                         │
│            │  - .research/task_plan.md    │                         │
│            └──────────────────────────────┘                         │
│                            ↓                                         │
│  会话2: /zhr-execute (独立会话)                                       │
│    读取状态 → Phase 3-5 → 更新状态                                    │
│                            ↓                                         │
│  会话3: /zhr-verify (独立会话)                                        │
│    读取状态 → Phase 6-7 → 输出 EXIT_SIGNAL                           │
│                                                                      │
│  ✅ 每个会话从干净的上下文开始                                         │
│  ✅ 长调研任务不会耗尽上下文窗口                                       │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🎯 你的唯一任务

执行 Phase 0-2，创建调研计划。
**完成后输出 `PHASE_COMPLETE: plan` 并结束会话。**
**不要执行 Phase 3+，等待外部脚本或用户调用 /zhr-execute。**

---

## 🧠 Phase 0: Graphiti 历史知识读取 (新增！)

### 0.1 检查 Graphiti 服务状态

```python
# 首先检查 Graphiti 是否可用
mcp__graphiti__get_status()

# 如果返回错误，提示用户启动服务
# 启动脚本: C:\Users\ROG\graphiti-official\start-graphiti.bat
```

### 0.2 读取历史调研记录

```python
# 搜索与用户需求相关的历史知识
mcp__graphiti__search_nodes(
    query="用户提到的关键词",
    group_ids=["spring-2026-courses", "zero-hallucination-research"],
    max_nodes=10
)

# 获取最近的调研 episodes
mcp__graphiti__get_episodes(
    group_ids=["spring-2026-courses", "zero-hallucination-research"],
    max_episodes=5
)

# 搜索相关事实
mcp__graphiti__search_memory_facts(
    query="用户提到的主题",
    group_ids=["spring-2026-courses"],
    max_facts=10
)
```

### 0.3 汇总历史知识

将读取到的历史知识汇总：
```markdown
## 🧠 历史知识汇总

### 已有的相关记录
- [从 Graphiti 读取的相关实体]
- [从 Graphiti 读取的相关事实]

### 最近的调研记录
- [最近的 episodes]

### 可以复用的信息
- [不需要重复调研的信息]

### 需要更新验证的信息
- [可能过时需要重新验证的信息]
```

---

## 🔀 Phase 0.5: 需求类型区分 (新增！)

### 必须询问用户

```
🔀 请确认本次调研的类型：

A) 📝 **迭代需求** - 在之前调研基础上继续/深化
   → 我会读取历史 PRD，增量更新
   → 历史需求和发现会被保留

B) 🆕 **全新需求** - 与之前调研无关的新主题
   → 我会创建全新 PRD
   → 但仍会参考历史知识避免重复工作

C) 🔄 **混合需求** - 部分继续，部分新增
   → 我会创建新版本 PRD (v1.1, v2.0...)
   → 保留相关历史，清理无关内容
```

### 根据选择执行不同逻辑

#### A) 迭代需求

```
1. 读取现有 .research/task_plan.md
2. 读取现有 .research/state.json
3. 将新需求追加到"增量需求"部分
4. 更新版本号 (v1.0 → v1.1)
5. 保留所有历史发现
```

#### B) 全新需求

```
1. 归档现有 .research/ 到 .research/archive/YYYY-MM-DD/
2. 创建全新 task_plan.md (v1.0)
3. 在 priority.md 中记录"历史参考"
4. 从 Graphiti 预加载相关知识
```

#### C) 混合需求

```
1. 读取现有 task_plan.md
2. 创建新版本 (v1.0 → v2.0 表示重大变更)
3. 标记哪些历史需求保留/废弃
4. 追加新需求
5. 保留相关发现，清理无关发现
```

---

## Phase 1: 需求界定

### 1.1 检查断点续传

```bash
# 检查是否存在未完成的调研
如果 .research/state.json 存在：
  → 读取当前状态
  → 询问用户："检测到上次未完成的调研 (v{版本})，选择：
      A) 继续上次调研
      B) 开始新调研（归档上次）
      C) 放弃上次调研"
```

### 1.2 询问用户确认

必须确认以下内容：
1. **调研主题列表**
2. **每个主题需要的信息类型**
3. **用户指定的来源 (📌)**
4. **验收标准**
5. **执行模式**：快速（后台并行）/ Chrome（前台顺序）

### 1.3 利用历史知识优化计划

基于 Phase 0 读取的历史知识：
- 跳过已经有可靠信息的主题（标注"复用历史"）
- 标记需要更新验证的信息
- 识别新增的调研范围

---

## Phase 2: 计划制定

### 2.1 创建/更新工作目录

```bash
mkdir -p .research/findings
mkdir -p .research/memory
mkdir -p .research/versions  # 新增：版本历史
mkdir -p .research/archive   # 新增：归档历史
```

### 2.2 创建/更新 task_plan.md

```markdown
# 调研任务计划

## 📋 版本信息
- **版本号**: v{X.Y}
- **创建时间**: {初次创建时间}
- **最后更新**: {当前时间}
- **需求类型**: {迭代/全新/混合}
- **基于版本**: {如果是迭代，记录上一版本}

---

## 🧠 历史知识复用

### 从 Graphiti 读取的已有知识
| 主题 | 已有信息 | 来源 | 有效期 | 是否复用 |
|------|---------|------|--------|---------|
| {主题} | {信息摘要} | {来源} | {valid_at} | ✅/🔄 需更新 |

### 需要重新调研的原因
- {原因1: 信息过时/需要更多细节/新需求}

---

## 🎯 核心需求

### 继承的需求（迭代模式）
- [✅ 已完成] {需求1} (来自 v{X.Y})
- [🔄 继续] {需求2} (来自 v{X.Y})

### 新增需求
- [ ] {新需求1}
- [ ] {新需求2}

---

## ✅ 验收标准

### 继承的标准
- [x] {标准1} (v{X.Y} 已满足)

### 新增标准
- [ ] {新标准1}

---

## 📌 用户指定来源

| 来源名称 | URL | 状态 | 继承自 |
|---------|-----|------|--------|
| 📌 {来源} | {URL} | ⏳ 待使用 | v{X.Y}/新增 |

---

## 📋 调研主题列表

| # | 主题 | 状态 | 数据来源 | 备注 |
|---|------|------|---------|------|
| 1 | {主题1} | 🧠 复用历史 | Graphiti | 无需重新调研 |
| 2 | {主题2} | 🔄 需更新 | Graphiti + 新调研 | 信息可能过时 |
| 3 | {主题3} | 🆕 新增 | 全新调研 | 无历史记录 |

---

## 📝 增量需求记录

### 版本演进历史
| 版本 | 日期 | 变更类型 | 说明 |
|------|------|---------|------|
| v1.0 | {日期} | 初始创建 | {描述} |
| v1.1 | {日期} | 增量迭代 | +{新需求} |
| v2.0 | {日期} | 重大变更 | {描述} |

### 本版本新增需求
- [✅ 已追加] {需求} (发现于: {时间})

---

## 🔍 幻觉记录
（发现的幻觉，防止重复）

---

## 💾 恢复信息

- **当前阶段**: Phase {N}
- **下一步操作**: {描述}
- **状态文件**: `.research/state.json`
- **版本历史**: `.research/versions/`
```

### 2.3 创建/更新 state.json (v2.0 独立会话架构)

**使用 v2.0 模板**（支持独立上下文窗口）：

```json
{
  "version": "2.0",
  "session_isolation": true,
  "current_phase": "plan",
  "phase_status": {
    "plan": "completed",
    "execute": "pending",
    "verify": "pending"
  },
  "prd": {
    "version": "1.0",
    "requirement_type": "iterate|new|mixed",
    "created_at": "YYYY-MM-DD HH:MM:SS",
    "updated_at": "YYYY-MM-DD HH:MM:SS",
    "file_path": ".research/task_plan.md"
  },
  "user_approvals": {
    "prd_approval": {
      "approved": true,
      "approved_at": "YYYY-MM-DD HH:MM:SS",
      "version": "1.0"
    }
  },
  "graphiti": {
    "service_checked": true,
    "knowledge_loaded": true,
    "nodes_found": 5,
    "facts_found": 10,
    "reused_topics": ["topic_1", "topic_2"],
    "new_topics": ["topic_3"]
  },
  "plan": {
    "started_at": "YYYY-MM-DD HH:MM:SS",
    "completed_at": "YYYY-MM-DD HH:MM:SS",
    "topics_defined": 3,
    "sources_specified": 5
  },
  "topics": [
    {
      "id": "topic_1",
      "name": "主题名称",
      "status": "reuse_history",
      "source": "graphiti",
      "notes": "无需重新调研"
    },
    {
      "id": "topic_2",
      "name": "主题名称",
      "status": "needs_update",
      "source": "graphiti+new",
      "notes": "信息可能过时"
    },
    {
      "id": "topic_3",
      "name": "主题名称",
      "status": "pending",
      "source": "new_research",
      "notes": "全新调研"
    }
  ],
  "session_history": [
    {
      "session_id": "plan-001",
      "phase": "plan",
      "started_at": "YYYY-MM-DD HH:MM:SS",
      "completed_at": "YYYY-MM-DD HH:MM:SS"
    }
  ],
  "last_updated": "YYYY-MM-DD HH:MM:SS"
}
```

### 2.4 保存版本历史

```bash
# 每次更新 PRD 时保存版本
cp .research/task_plan.md .research/versions/task_plan_v{X.Y}.md
```

### 2.5 创建 priority.md

```markdown
# 优先上下文（每次启动必读）

## 版本信息
- 当前版本: v{X.Y}
- 需求类型: {迭代/全新/混合}

## 核心需求
[从 task_plan.md 复制]

## 验收标准
[从 task_plan.md 复制]

## 🧠 历史知识参考
以下信息来自 Graphiti，可以直接使用：
- {已验证的历史信息}

## 关键约束
- 所有信息必须标注 [事实]/[推理]/[未验证]
- 所有来源必须标注 📌/🔍
- 无法获取的信息必须直接询问用户
- **调研前先查 Graphiti 是否有现有知识！**
```

---

## ✅ 完成检查

计划阶段完成后，必须确认：
- [ ] Graphiti 历史知识已读取
- [ ] 需求类型已区分（迭代/全新/混合）
- [ ] `.research/task_plan.md` 已创建/更新
- [ ] `.research/state.json` 已创建/更新（v2.0 格式）
- [ ] `state.json.phase_status.plan` = "completed"
- [ ] `state.json.user_approvals.prd_approval.approved` = true
- [ ] `.research/memory/priority.md` 已创建
- [ ] 版本历史已保存到 `.research/versions/`
- [ ] 用户已确认所有核心需求和验收标准

---

## 📢 完成后输出（强制格式）

**必须输出以下内容，供外部脚本解析：**

```
═══════════════════════════════════════════════════════════════════════
PHASE_COMPLETE: plan
═══════════════════════════════════════════════════════════════════════

✅ 计划阶段完成！

版本: v{X.Y} ({迭代/全新/混合}模式)

Graphiti 历史知识:
- 可复用信息: {N} 条
- 需更新信息: {N} 条
- 全新调研: {N} 个主题

已创建/更新：
- .research/task_plan.md（任务计划 v{X.Y}）
- .research/state.json（执行状态 v2.0）
- .research/memory/priority.md（优先上下文）
- .research/versions/task_plan_v{X.Y}.md（版本存档）

状态文件检查点：
- phase_status.plan: completed
- user_approvals.prd_approval.approved: true

═══════════════════════════════════════════════════════════════════════
下一步：
  - 手动执行: /zhr-execute
  - 或使用外部脚本: ralph-zhr.bat (Windows) / ralph-zhr.sh (Linux/Mac)
═══════════════════════════════════════════════════════════════════════
```

---

## 🛑 会话结束规则

**⛔ 输出 `PHASE_COMPLETE: plan` 后，本会话必须结束！**

- **禁止**继续执行 Phase 3+
- **禁止**在同一会话中调用 /zhr-execute
- **等待**外部脚本或用户在**新会话**中调用 /zhr-execute

**原因**：保持独立上下文窗口，避免上下文累积。
